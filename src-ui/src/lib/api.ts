/**
 * Tauri IPC invoke wrappers.
 *
 * Provides typed wrapper functions around `invoke()` calls
 * to the Rust backend. Each function corresponds to a Tauri
 * command registered in src-tauri/src/commands/.
 */
